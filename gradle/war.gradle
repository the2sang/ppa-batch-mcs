apply plugin: "war"

bootWar {
    mainClassName = "com.kepco.ppa.web.PPABatchApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
